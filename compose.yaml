services:
  redis:
    image: redis:6.2-alpine
    ports:
      - "6379:6379"

  spring-app:
    build: .
    environment:
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
      APP_REDIS_ENABLED: "true"
    depends_on:
      - redis
    ports:
      - "8080:8080"

